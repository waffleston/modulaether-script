// Hello, there!
%^comments off
// This comment cannot be seen!
// %^comments on
// Still off.
%^comments on
// Now you can see them (again)!
// %^comments off
// Now you can see the above instruction did not execute.
%^comments off
%^creturn off

"Line comment variations";
var line1 = function(){}//comment here mates.
var line2 = function(){} //comment here mates.
var line3 = function(){}// Comment here mates.
var line4 = function(){} //.
var line5 = function(){} //
var line6 = function(){} //hello
var line7 = function(){}//

/**
 * @function
 * Returns some memes, I guess.
 */
function returnMemes(one,two) {
	//We have some memes here.
	var one = $('#body').value;
	var two = $('#head').value;
	selectMemes()
	var shrubs = {
		ha: 1,
		h2: 2
	}
}/*
function uwotm8(m,m8) {
	help;
}
*/
%^defer 3000
%^srcdef fileone
%^srcdef filetwo
%^fn fileone selectMemes(one,two) {
	//Let's figure out what the memes are.
	console.log("one: ", one);
	console.log("two: ", two);
	var aboi = {
		hi: 0,
		ne: 1
	}
}

select/*hi*/Memes(/**/1,2);
%^creturn on
/*
	Tabs are now recognized.
	So are        multiple spaces!
*/
%^root /HTML/core/
%^srcdef from_memes

console.log(" %^insert inserted.txt ");
